/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: '/YOGAS7STATIC',
    trailingSlash: true,
    images: {
        unoptimized: true
    }

};

export default nextConfig;
